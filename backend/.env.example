# Only needed for local scripts (e.g. scripts/seed-watches.ts) run outside CDK deploy.
# In deployed Lambdas these are set as environment variables by the CDK stacks themselves.

AWS_REGION=us-east-1
WATCHES_TABLE=checkon-watches
SUBSCRIPTIONS_TABLE=checkon-subscriptions
CHANGES_TABLE=checkon-changes
DIGEST_QUEUE_TABLE=checkon-digest-queue
SNAPSHOTS_BUCKET=
PUBLIC_SITE_BUCKET=
PUBLIC_SITE_URL=http://localhost:3000
SES_FROM_ADDRESS=alerts@checkon.app
BEDROCK_MODEL_ID=anthropic.claude-haiku-4-5-20251001-v1:0
